---
title: Update service application
full: true
_openapi:
  method: PATCH
  route: /services/{uuid}/applications/{app_uuid}
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Update fields for a compose service application. Use `url` for
          comma-separated public URLs (same rules as `urls[].url` on PATCH
          /services/{uuid}).
---

Update fields for a compose service application. Use `url` for comma-separated public URLs (same rules as `urls[].url` on PATCH /services/{uuid}).

<APIPage document={"./openapi.yaml"} operations={[{"path":"/services/{uuid}/applications/{app_uuid}","method":"patch"}]} webhooks={[]} hasHead={false} />