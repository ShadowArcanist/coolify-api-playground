---
title: Start or redeploy service application container
full: true
_openapi:
  method: POST
  route: /services/{uuid}/applications/{app_uuid}/start
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Runs docker compose up for a single compose service (no-deps),
          optionally pulling the image and rebuilding.
---

Runs docker compose up for a single compose service (no-deps), optionally pulling the image and rebuilding.

<APIPage document={"./openapi.yaml"} operations={[{"path":"/services/{uuid}/applications/{app_uuid}/start","method":"post"}]} webhooks={[]} hasHead={false} />